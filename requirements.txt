# This file is used by Docker/Airflow containers.
# Source of truth is pyproject.toml (managed with uv).
# Regenerate with:
# uv export --no-hashes --format requirements-txt -o requirements.txt
requests
boto3
connexion[swagger-ui]>=2.10.0,<3.0
kafka-python
psycopg[binary]
pandas
pytest
apache-airflow==2.8.1